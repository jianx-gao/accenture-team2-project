# Backend Server Configuration
# =============================

# The IP address the backend server will listen on
# - Use 'localhost' or '127.0.0.1' for local-only access (default)
# - Use '0.0.0.0' to listen on all network interfaces (accessible from other devices on network)
# - Use a specific IP address to bind to a particular network interface
SERVER_HOST=localhost

# The port the backend server will listen on
# Default: 3001
# Choose any available port (1024-65535 recommended for non-privileged users)
SERVER_PORT=3001

# Environment mode (development, production, test)
NODE_ENV=development

# Frontend Configuration
# ======================

# The URL where the frontend is hosted (for CORS)
# This must match the origin of your frontend application
# Default: http://localhost:3000 (Vite dev server default)
FRONTEND_URL=http://localhost:3000

# AI Configuration (for future integration)
# =========================================
# AI_API_KEY=your_api_key_here
# AI_MODEL=gpt-4

